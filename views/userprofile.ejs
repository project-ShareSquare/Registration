<!DOCTYPE html>
<html>
<head>
    <title>User Profile</title>
    <style>
        body {
            background: url('https://png.pngtree.com/background/20210716/original/pngtree-light-blue-cute-striped-baby-blue-background-picture-image_1348681.jpg') center/cover;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .user-profile {
            background: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            position: relative; /* Add this line to the user-profile */
        }
        .home-button {
            position: absolute;
            top: 20px;
            left: 20px;
        }
    </style>
</head>
<body>
    <div class="user-profile">
        <a href="/home" class="home-button">Home</a> <!-- Add the home button -->
        <h1>Welcome to your User Profile, <%= user.name %>!</h1>
        <!-- Display user information and other content here -->
        <p>Email: <%= user.email %></p>
        <!-- Other user-related information -->
        <form method="post" action="/logout?_method=DELETE">
            <button type="submit">Logout</button>
        </form>
    </div>
</body>
</html>

